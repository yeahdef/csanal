def load_team_stats():
    global teams #make teams global variable
    global pandas_teams #glbal pandas teams
    teams = set([])
    teams_used = set([])
    teams_matches = []
    for w in parsed_json['matches']:
        teams.add(w['team_a'])
        for t in teams:
            if t == w['winner']:
                ##how do i make a dictionary for each new set of 't' == w['winner'] now?
                
                print '{0}{1}'
